import { createTheme } from "@mui/material/styles";

const palette = {
  primary: {
    main: "#2557a7",
  },
};

const theme = {
  palette,
};

const indeedTheme = createTheme(theme);

export default indeedTheme;
